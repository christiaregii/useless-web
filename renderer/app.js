/**
 * Person A — App entry point
 */

document.addEventListener('DOMContentLoaded', () => {
  console.log('[App] Initializing Watch Party application...');
  window.appIntegration = new window.Integration();
  console.log('[App] Ready.');
});
